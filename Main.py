from Triangulo import Triangulo
from Circulo import Circulo
from Rectangulo import Rectangulo
from Figuras import Figura

if __name__ == '__main__':
    #Crear instancia del circulo
    #mi_circulo = Circulo(5)
    #print(mi_circulo.calcular_area())

    #Crear instancia del Triangulo
    #mi_triangulo = Triangulo(45,90)
    #print(mi_triangulo.calcular_area())

    #Crear instancia del Rectangulo
    #mi_rectangulo = Rectangulo(34,67)
    #print(mi_rectangulo.calcular_area())

    #figuras=[Circulo(5), Triangulo(45,90), Rectangulo(34,67)]
    #for figura in figuras:
     #   figura.calcular_area()
      #  print(f"Nombre:{figura.nombre}, Area:{figura.area})")
      


